name: Success Fail YML

on: 
  push:
  workflow_dispatch:

jobs:
  test-success-fail:
    name: 'Test success fail'
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Success 1'
      - run: echo 'Success 2'
      - run: echo 'Success 3'
      - if: success()
        run: echo 'Test Aman 1'
      - run: exit 1
      - run: echo 'Success 4'
      - if: failure()
        run: echo 'Success 5'
      - if: failure()
        run: echo 'Success 6'
      - if: success()
        run: echo 'Test Aman 2'
      - run: echo 'Success 6'
      - if: success()
        run: echo 'Test Aman 3'