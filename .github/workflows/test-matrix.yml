name: Matrix Test

on:
  workflow_dispatch:
  push:

jobs:
  matrix-test:
    name: pre-commit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 20
      - run: exit 1
